language: node_js

sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

    packages:
      - gcc-4.8
      - g++-4.8
      - tcl-dev

node_js:
  - "0.10"
  - "node"
  - "iojs"

script:
  - npm run lint
  - npm test

after_success:
  - npm run coveralls

